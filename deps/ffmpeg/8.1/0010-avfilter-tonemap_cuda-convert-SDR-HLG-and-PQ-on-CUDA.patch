From c9985a81d80f939c460515c9d311be73b4a97a55 Mon Sep 17 00:00:00 2001
From: avplumber contributors <patches@local>
Date: Wed, 16 Sep 2026 20:08:18 +0200
Subject: [PATCH] avfilter/tonemap_cuda: convert SDR, HLG and PQ on CUDA

---
 doc/filters.texi               |  66 +++++++++++++
 libavfilter/vf_tonemap_cuda.c  |  93 ++++++++++++++----
 libavfilter/vf_tonemap_cuda.cu | 170 +++++++++++++++++++++++++--------
 3 files changed, 274 insertions(+), 55 deletions(-)

diff --git a/doc/filters.texi b/doc/filters.texi
index 5ae2fc5080..021d649836 100644
--- a/doc/filters.texi
+++ b/doc/filters.texi
@@ -27409,6 +27409,72 @@ scale_cuda=passthrough=0
 @end example
 @end itemize
 
+@subsection tonemap_cuda
+
+Convert limited-range CUDA NV12/P010 frames between SDR BT.709, HLG BT.2020
+and PQ BT.2020. Width and height must be even. HDR output uses P010; SDR
+output uses NV12. Equal input and output transfers forward the original
+frames and metadata without allocating or converting pixels.
+
+@table @option
+@item transfer_in
+@item transfer_out
+Set both options to @code{sdr}, @code{hlg} or @code{pq}. The choice includes
+the gamut: BT.709 for SDR, BT.2020 non-constant-luminance YCbCr for HDR.
+Explicit choices also define the interpretation of untagged input. Full-range
+input must first be converted to limited range (e.g. with colorspace_cuda).
+
+SDR uses the BT.1886 display response with ideal black (gamma 2.4). SDR-to-HDR
+conversion follows display-light mapping: decode SDR, convert primaries,
+scale reference white, then encode PQ or the inverse HLG EOTF. It preserves
+the SDR appearance without expanding highlights. HLG/PQ conversion preserves
+display luminance, clipping values outside the destination signal range.
+
+@item sdr_white
+SDR reference white in cd/m2. Default 203. Range 1 to 10000; must not exceed
+@option{hdr_peak}. This sets SDR's brightness inside an HDR programme.
+
+@item hdr_peak
+HLG nominal display peak and HDR-to-SDR tone-mapping input peak in cd/m2.
+Default 1000, range 100 to 10000. PQ values always represent absolute light;
+this option does not rescale PQ. For HLG, the system gamma is derived from
+this peak, with a minimum gamma of 1.0.
+
+@item tonemap
+HDR-to-SDR operator: @code{none}, @code{linear}, @code{gamma}, @code{clip},
+@code{reinhard}, @code{hable} (default) or @code{mobius}. Used only when
+converting HDR to SDR. @code{none:desat=0} preserves display light up to SDR
+white, then clips. @option{param} overrides the operator parameter; @option{desat}
+controls highlight desaturation (default 0.5; 0 disables it).
+
+@item transfer
+@item t
+Legacy input transfer, @code{hlg} (default) or @code{pq}. Without
+@option{transfer_in}/@option{transfer_out}, retain the original P010-to-NV12
+HDR-to-SDR mapping, including the BT.709 output curve. The legacy
+@option{peak} is in units of 100 cd/m2 (default 10). Do not combine this
+interface with the explicit transfer pair. @option{sdr_white} and
+@option{hdr_peak} affect only explicit conversions.
+@end table
+
+Converted frames carry the output color tags; obsolete mastering, content
+light and dynamic HDR metadata is removed. Pass-through preserves all metadata.
+
+For an SDR source entering an HLG P210 mixer, use this GPU-only source chain:
+@example
+tonemap_cuda=transfer_in=sdr:transfer_out=hlg:sdr_white=203:hdr_peak=1000,scale_cuda=format=p210le
+@end example
+
+To convert HLG to PQ without tone mapping:
+@example
+tonemap_cuda=transfer_in=hlg:transfer_out=pq:hdr_peak=1000
+@end example
+
+To produce SDR from PQ using the Hable operator:
+@example
+tonemap_cuda=transfer_in=pq:transfer_out=sdr:tonemap=hable:sdr_white=203:hdr_peak=1000
+@end example
+
 @subsection thumbnail_cuda
 
 Select the most representative frame in a given sequence of consecutive frames using CUDA.
diff --git a/libavfilter/vf_tonemap_cuda.c b/libavfilter/vf_tonemap_cuda.c
index 527cb0e96f..2d7eea2996 100644
--- a/libavfilter/vf_tonemap_cuda.c
+++ b/libavfilter/vf_tonemap_cuda.c
@@ -18,9 +18,8 @@
 
 /**
  * @file
- * HDR to SDR tone mapping using CUDA hardware acceleration. This is a CUDA port
- * of vf_tonemap_opencl restricted to BT.2020 HLG/PQ P010 input and BT.709 SDR
- * NV12 output (both 10-bit and 8-bit 4:2:0, so no chroma resampling).
+ * SDR BT.709 / HDR BT.2020 HLG and PQ conversion on CUDA 4:2:0 frames.
+ * The legacy transfer/t interface retains the tonemap_opencl-derived mapping.
  */
 
 #include <float.h>
@@ -59,6 +58,7 @@ enum TonemapAlgorithm {
 enum TonemapTransfer {
     TONEMAP_TRANSFER_HLG,
     TONEMAP_TRANSFER_PQ,
+    TONEMAP_TRANSFER_SDR,
 };
 
 /* Maps the public TonemapAlgorithm enum onto the operator codes understood by
@@ -90,6 +90,14 @@ typedef struct TonemapCUDAContext {
     int    tonemap;
     /* enum TonemapTransfer (input transfer characteristic) */
     int    transfer;
+    int    transfer_in;
+    int    transfer_out;
+    int    legacy;
+    int    passthrough;
+    int    input_depth;
+    int    output_depth;
+    double sdr_white;
+    double hdr_peak;
     double peak;
     double param;
     double desat;
@@ -99,6 +107,17 @@ static av_cold int tonemap_cuda_init(AVFilterContext *ctx)
 {
     TonemapCUDAContext *s = ctx->priv;
 
+    s->legacy = s->transfer_in < 0 && s->transfer_out < 0;
+    if (!s->legacy && (s->transfer_in < 0 || s->transfer_out < 0 || s->transfer >= 0)) {
+        av_log(ctx, AV_LOG_ERROR,
+               "Set both transfer_in and transfer_out, without legacy transfer/t\n");
+        return AVERROR(EINVAL);
+    }
+    if (s->legacy) {
+        s->transfer_in = s->transfer < 0 ? TONEMAP_TRANSFER_HLG : s->transfer;
+        s->transfer_out = TONEMAP_TRANSFER_SDR;
+    }
+
     s->own_frame = av_frame_alloc();
     if (!s->own_frame)
         return AVERROR(ENOMEM);
@@ -143,7 +162,7 @@ static av_cold int init_hwframe_ctx(TonemapCUDAContext *s, AVBufferRef *device_c
     out_ctx = (AVHWFramesContext*)out_ref->data;
 
     out_ctx->format    = AV_PIX_FMT_CUDA;
-    out_ctx->sw_format = AV_PIX_FMT_NV12;
+    out_ctx->sw_format = s->output_depth == 10 ? AV_PIX_FMT_P010 : AV_PIX_FMT_NV12;
     out_ctx->width     = FFALIGN(width, 32);
     out_ctx->height    = FFALIGN(height, 32);
 
@@ -214,12 +233,24 @@ static av_cold int tonemap_cuda_config_output(AVFilterLink *outlink)
 
     in_frames_ctx = (AVHWFramesContext*)inl->hw_frames_ctx->data;
 
-    if (in_frames_ctx->sw_format != AV_PIX_FMT_P010) {
+    if (in_frames_ctx->sw_format != AV_PIX_FMT_P010 &&
+        (s->legacy || in_frames_ctx->sw_format != AV_PIX_FMT_NV12)) {
         av_log(ctx, AV_LOG_ERROR,
-               "Unsupported input format: %s, only p010 is supported\n",
+               "Unsupported input format: %s (legacy mode needs P010; explicit mode accepts NV12/P010)\n",
                av_get_pix_fmt_name(in_frames_ctx->sw_format));
         return AVERROR(ENOSYS);
     }
+    if ((inlink->w & 1) || (inlink->h & 1)) {
+        av_log(ctx, AV_LOG_ERROR, "CUDA 4:2:0 conversion requires even dimensions\n");
+        return AVERROR(EINVAL);
+    }
+    if (!s->legacy && s->sdr_white > s->hdr_peak) {
+        av_log(ctx, AV_LOG_ERROR, "sdr_white must not exceed hdr_peak\n");
+        return AVERROR(EINVAL);
+    }
+    s->input_depth = in_frames_ctx->sw_format == AV_PIX_FMT_P010 ? 10 : 8;
+    s->output_depth = s->transfer_out == TONEMAP_TRANSFER_SDR ? 8 : 10;
+    s->passthrough = !s->legacy && s->transfer_in == s->transfer_out;
 
     /* Resolve the per-operator default tone mapping parameter. */
     if (isnan(s->param)) {
@@ -244,6 +275,11 @@ static av_cold int tonemap_cuda_config_output(AVFilterLink *outlink)
     outlink->h = inlink->h;
     outlink->sample_aspect_ratio = inlink->sample_aspect_ratio;
 
+    if (s->passthrough) {
+        outl->hw_frames_ctx = av_buffer_ref(inl->hw_frames_ctx);
+        return outl->hw_frames_ctx ? 0 : AVERROR(ENOMEM);
+    }
+
     ret = init_hwframe_ctx(s, in_frames_ctx->device_ref, inlink->w, inlink->h);
     if (ret < 0)
         return ret;
@@ -271,7 +307,13 @@ static int tonemap_cuda_conv(AVFilterContext *ctx, AVFrame *out, AVFrame *in)
     int cw = width  / 2;
     int ch = height / 2;
 
-    int   transfer = s->transfer;
+    int   transfer = s->transfer_in;
+    int   transfer_out = s->transfer_out;
+    int   legacy = s->legacy;
+    int   input_depth = s->input_depth;
+    int   output_depth = s->output_depth;
+    float sdr_white = s->sdr_white;
+    float hdr_peak = s->hdr_peak;
     int   op       = tonemap_op_map[s->tonemap];
     float peak     = s->peak;
     float param    = s->param;
@@ -283,7 +325,8 @@ static int tonemap_cuda_conv(AVFilterContext *ctx, AVFrame *out, AVFrame *in)
         &s->own_frame->data[0], &s->own_frame->linesize[0],
         &s->own_frame->data[1], &s->own_frame->linesize[1],
         &width, &height,
-        &transfer, &peak, &op, &param, &desat
+        &transfer, &peak, &op, &param, &desat,
+        &transfer_out, &legacy, &input_depth, &output_depth, &sdr_white, &hdr_peak
     };
 
     ret = CHECK_CU(cu->cuLaunchKernel(s->cu_func,
@@ -307,11 +350,15 @@ static int tonemap_cuda_conv(AVFilterContext *ctx, AVFrame *out, AVFrame *in)
     if (ret < 0)
         return ret;
 
-    /* The output frame is now BT.709 SDR limited-range NV12. */
-    out->color_trc       = AVCOL_TRC_BT709;
-    out->color_primaries = AVCOL_PRI_BT709;
-    out->colorspace      = AVCOL_SPC_BT709;
+    out->color_trc = s->transfer_out == TONEMAP_TRANSFER_SDR ? AVCOL_TRC_BT709 :
+                    s->transfer_out == TONEMAP_TRANSFER_HLG ? AVCOL_TRC_ARIB_STD_B67 :
+                                                            AVCOL_TRC_SMPTE2084;
+    out->color_primaries = s->transfer_out == TONEMAP_TRANSFER_SDR ? AVCOL_PRI_BT709 : AVCOL_PRI_BT2020;
+    out->colorspace      = s->transfer_out == TONEMAP_TRANSFER_SDR ? AVCOL_SPC_BT709 : AVCOL_SPC_BT2020_NCL;
     out->color_range     = AVCOL_RANGE_MPEG;
+    /* Source mastering / dynamic HDR metadata no longer describes these pixels. */
+    av_frame_side_data_remove_by_props(&out->side_data, &out->nb_side_data,
+                                       AV_SIDE_DATA_PROP_COLOR_DEPENDENT);
 
     return 0;
 }
@@ -327,6 +374,15 @@ static int tonemap_cuda_filter_frame(AVFilterLink *link, AVFrame *in)
     CUcontext dummy;
     int ret = 0;
 
+    if (s->passthrough)
+        return ff_filter_frame(outlink, in);
+
+    if (!s->legacy && in->color_range == AVCOL_RANGE_JPEG) {
+        av_log(ctx, AV_LOG_ERROR, "Full-range input is unsupported; convert to limited range first\n");
+        ret = AVERROR(EINVAL);
+        goto fail;
+    }
+
     out = av_frame_alloc();
     if (!out) {
         ret = AVERROR(ENOMEM);
@@ -362,11 +418,16 @@ static const AVOption tonemap_cuda_options[] = {
         { "reinhard", 0, 0, AV_OPT_TYPE_CONST, { .i64 = TONEMAP_REINHARD }, 0, 0, FLAGS, .unit = "tonemap" },
         { "hable",    0, 0, AV_OPT_TYPE_CONST, { .i64 = TONEMAP_HABLE },    0, 0, FLAGS, .unit = "tonemap" },
         { "mobius",   0, 0, AV_OPT_TYPE_CONST, { .i64 = TONEMAP_MOBIUS },   0, 0, FLAGS, .unit = "tonemap" },
-    { "transfer", "set input transfer characteristic", OFFSET(transfer), AV_OPT_TYPE_INT, { .i64 = TONEMAP_TRANSFER_HLG }, TONEMAP_TRANSFER_HLG, TONEMAP_TRANSFER_PQ, FLAGS, .unit = "transfer" },
-    { "t",        "set input transfer characteristic", OFFSET(transfer), AV_OPT_TYPE_INT, { .i64 = TONEMAP_TRANSFER_HLG }, TONEMAP_TRANSFER_HLG, TONEMAP_TRANSFER_PQ, FLAGS, .unit = "transfer" },
+    { "transfer", "legacy HDR input transfer (default HLG)", OFFSET(transfer), AV_OPT_TYPE_INT, { .i64 = -1 }, -1, TONEMAP_TRANSFER_PQ, FLAGS, .unit = "transfer" },
+    { "t",        "legacy HDR input transfer (default HLG)", OFFSET(transfer), AV_OPT_TYPE_INT, { .i64 = -1 }, -1, TONEMAP_TRANSFER_PQ, FLAGS, .unit = "transfer" },
+    { "transfer_in", "input: sdr (BT.709/BT.1886), hlg or pq (BT.2020)", OFFSET(transfer_in), AV_OPT_TYPE_INT, { .i64 = -1 }, -1, TONEMAP_TRANSFER_SDR, FLAGS, .unit = "transfer" },
+    { "transfer_out", "output: sdr (NV12), hlg or pq (P010); same transfer passes through", OFFSET(transfer_out), AV_OPT_TYPE_INT, { .i64 = -1 }, -1, TONEMAP_TRANSFER_SDR, FLAGS, .unit = "transfer" },
         { "hlg", "ARIB STD-B67 (Hybrid Log-Gamma)", 0, AV_OPT_TYPE_CONST, { .i64 = TONEMAP_TRANSFER_HLG }, 0, 0, FLAGS, .unit = "transfer" },
         { "pq",  "SMPTE ST 2084 (Perceptual Quantizer)", 0, AV_OPT_TYPE_CONST, { .i64 = TONEMAP_TRANSFER_PQ }, 0, 0, FLAGS, .unit = "transfer" },
-    { "peak",  "signal peak override in units of reference white", OFFSET(peak),  AV_OPT_TYPE_DOUBLE, { .dbl = 10.0 }, 0,       DBL_MAX, FLAGS },
+        { "sdr", "BT.709 gamut with BT.1886 display response", 0, AV_OPT_TYPE_CONST, { .i64 = TONEMAP_TRANSFER_SDR }, 0, 0, FLAGS, .unit = "transfer" },
+    { "sdr_white", "SDR reference white in nits (explicit mode)", OFFSET(sdr_white), AV_OPT_TYPE_DOUBLE, { .dbl = 203.0 }, 1, 10000, FLAGS },
+    { "hdr_peak", "HLG display peak / HDR tone mapping peak in nits (explicit mode)", OFFSET(hdr_peak), AV_OPT_TYPE_DOUBLE, { .dbl = 1000.0 }, 100, 10000, FLAGS },
+    { "peak",  "legacy signal peak in units of 100 nits", OFFSET(peak), AV_OPT_TYPE_DOUBLE, { .dbl = 10.0 }, 0.001, DBL_MAX, FLAGS },
     { "param", "tonemap parameter",                                OFFSET(param), AV_OPT_TYPE_DOUBLE, { .dbl = NAN },  DBL_MIN, DBL_MAX, FLAGS },
     { "desat", "desaturation parameter",                           OFFSET(desat), AV_OPT_TYPE_DOUBLE, { .dbl = 0.5 },  0,       DBL_MAX, FLAGS },
     { NULL }
@@ -392,7 +453,7 @@ static const AVFilterPad tonemap_cuda_outputs[] = {
 
 const FFFilter ff_vf_tonemap_cuda = {
     .p.name         = "tonemap_cuda",
-    .p.description  = NULL_IF_CONFIG_SMALL("Perform HDR to SDR conversion with tonemapping using CUDA"),
+    .p.description  = NULL_IF_CONFIG_SMALL("Convert SDR, HLG and PQ with tonemapping using CUDA"),
     .p.priv_class   = &tonemap_cuda_class,
     .priv_size      = sizeof(TonemapCUDAContext),
     .init           = tonemap_cuda_init,
diff --git a/libavfilter/vf_tonemap_cuda.cu b/libavfilter/vf_tonemap_cuda.cu
index 8a4b9e6aa4..0bddb7c671 100644
--- a/libavfilter/vf_tonemap_cuda.cu
+++ b/libavfilter/vf_tonemap_cuda.cu
@@ -18,11 +18,9 @@
 
 /*
  * CUDA HDR->SDR tone mapper, ported from FFmpeg libavfilter/opencl/tonemap.cl
- * and colorspace_common.cl. It maps BT.2020 HLG or PQ 10-bit P010 frames to
- * BT.709 SDR 8-bit NV12 frames using a static signal peak (no per-frame peak
- * detection). The device functions below mirror the OpenCL kernel restricted
- * to the P010->NV12 4:2:0 layout: one work item processes a 2x2 luma block and
- * the single chroma sample it shares.
+ * and colorspace_common.cl, with display-light SDR/HLG/PQ conversion.
+ * The legacy math is retained for transfer/t callers. Explicit conversions
+ * use limited-range NV12/P010 and SDR BT.1886 with an ideal black display.
  */
 
 #define REFERENCE_WHITE 100.0f
@@ -161,19 +159,121 @@ __device__ static inline float3 rgb2yuv_709(float3 c) {
     return make_float3(y, u, v);
 }
 
-__device__ static inline unsigned char quant8(float v) {
-    return (unsigned char)fminf(fmaxf(rintf(v), 0.0f), 255.0f);
+__device__ static inline float3 mul_rgb(float3 c, float scale) {
+    return make_float3(c.x * scale, c.y * scale, c.z * scale);
 }
 
-// One work item maps a 2x2 luma block plus the shared chroma sample from P010
-// (BT.2020 HLG/PQ) to NV12 (BT.709 SDR). Linesizes are given in bytes.
+// Display-light mapping: SDR BT.1886 (ideal black), PQ ST 2084, HLG BT.2100.
+// Linear RGB uses the same units of 100 nits as the legacy path above.
+__device__ static inline float3 to_display_light(float3 c, int transfer,
+                                                float sdr_white, float hdr_peak) {
+    c = make_float3(fmaxf(c.x, 0.0f), fmaxf(c.y, 0.0f), fmaxf(c.z, 0.0f));
+    if (transfer == 2)
+        return mul_rgb(make_float3(__powf(c.x, 2.4f), __powf(c.y, 2.4f), __powf(c.z, 2.4f)),
+                       sdr_white / REFERENCE_WHITE);
+    if (transfer == 1)
+        return make_float3(eotf_st2084(c.x), eotf_st2084(c.y), eotf_st2084(c.z));
+    return ootf_hlg(make_float3(inverse_oetf_hlg(c.x), inverse_oetf_hlg(c.y),
+                               inverse_oetf_hlg(c.z)), hdr_peak / REFERENCE_WHITE);
+}
+
+__device__ static inline float oetf_pq(float c) {
+    float p = __powf(fmaxf(c, 0.0f) * REFERENCE_WHITE / ST2084_MAX_LUMINANCE, ST2084_M1);
+    return __powf((ST2084_C1 + ST2084_C2 * p) / (1.0f + ST2084_C3 * p), ST2084_M2);
+}
+
+// Inverse of inverse_oetf_hlg: scene RGB here is normalized to [0,12].
+__device__ static inline float oetf_hlg(float c) {
+    c = fmaxf(c, 0.0f);
+    return c <= 1.0f ? 0.5f * sqrtf(c) : HLG_A * __logf(c - HLG_B) + HLG_C;
+}
+
+__device__ static inline float3 from_display_light(float3 c, int transfer,
+                                                  float sdr_white, float hdr_peak) {
+    c = make_float3(fmaxf(c.x, 0.0f), fmaxf(c.y, 0.0f), fmaxf(c.z, 0.0f));
+    if (transfer == 2) {
+        c = mul_rgb(c, REFERENCE_WHITE / sdr_white);
+        return make_float3(__powf(c.x, 1.0f / 2.4f), __powf(c.y, 1.0f / 2.4f),
+                           __powf(c.z, 1.0f / 2.4f));
+    }
+    if (transfer == 1)
+        return make_float3(oetf_pq(c.x), oetf_pq(c.y), oetf_pq(c.z));
+    float gamma = fmaxf(1.0f, 1.2f + 0.42f * __log10f(hdr_peak / 1000.0f));
+    float luma = luma_2020(c);
+    if (luma <= 0.0f)
+        return make_float3(0.0f, 0.0f, 0.0f);
+    // BT.2100 inverse OOTF, preserving chromaticity in display light.
+    float scene_luma = 12.0f * __powf(luma * REFERENCE_WHITE / hdr_peak, 1.0f / gamma);
+    c = mul_rgb(c, scene_luma / luma);
+    return make_float3(oetf_hlg(c.x), oetf_hlg(c.y), oetf_hlg(c.z));
+}
+
+__device__ static inline float3 lrgb709_to_2020(float3 c) {
+    return make_float3(0.627404f * c.x + 0.329283f * c.y + 0.043313f * c.z,
+                       0.069097f * c.x + 0.919540f * c.y + 0.011362f * c.z,
+                       0.016391f * c.x + 0.088013f * c.y + 0.895595f * c.z);
+}
+
+__device__ static inline float3 convert_rgb(float3 c, int transfer_in, int transfer_out,
+                                           float sdr_white, float hdr_peak,
+                                           int op, float param, float desat) {
+    c = to_display_light(c, transfer_in, sdr_white, hdr_peak);
+    if (transfer_in == 2 && transfer_out != 2)
+        c = lrgb709_to_2020(c);
+    else if (transfer_in != 2 && transfer_out == 2) {
+        c = lrgb2020_to_709(c);
+        // Only HDR->SDR compresses highlights. SDR embedding and HLG/PQ
+        // conversion preserve display luminance, without inventing highlights.
+        c = mul_rgb(c, REFERENCE_WHITE / sdr_white);
+        c = map_one_pixel_rgb(c, hdr_peak / sdr_white, SDR_AVG, op, param, desat);
+        c = mul_rgb(c, sdr_white / REFERENCE_WHITE);
+    }
+    c = from_display_light(c, transfer_out, sdr_white, hdr_peak);
+    // Clip outside the target signal range before forming YCbCr.
+    return make_float3(fminf(c.x, 1.0f), fminf(c.y, 1.0f), fminf(c.z, 1.0f));
+}
+
+__device__ static inline float3 yuv_to_rgb(float y, float u, float v, int depth, int transfer) {
+    float scale = depth == 10 ? 4.0f : 1.0f;
+    y = (y / scale - 16.0f) / 219.0f;
+    u = (u / scale - 128.0f) / 224.0f;
+    v = (v / scale - 128.0f) / 224.0f;
+    if (transfer == 2)
+        return make_float3(y + 1.5748f * v, y - 0.187324f * u - 0.468124f * v, y + 1.8556f * u);
+    return make_float3(y + 1.4746f * v, y - 0.164553f * u - 0.571353f * v, y + 1.8814f * u);
+}
+
+__device__ static inline float3 rgb_to_yuv(float3 c, int transfer) {
+    if (transfer == 2)
+        return rgb2yuv_709(c);
+    float y = luma_2020(c);
+    return make_float3(y, (c.z - y) / 1.8814f, (c.x - y) / 1.4746f);
+}
+
+__device__ static inline float read_code(const unsigned char *p, int pitch, int x, int y, int depth) {
+    const unsigned char *row = p + y * pitch;
+    return depth == 10 ? ((const unsigned short *)row)[x] >> 6 : row[x];
+}
+
+__device__ static inline void write_code(unsigned char *p, int pitch, int x, int y, int depth, float code) {
+    unsigned char *row = p + y * pitch;
+    if (depth == 10)
+        ((unsigned short *)row)[x] = (unsigned short)fminf(fmaxf(rintf(code * 4.0f), 0.0f), 1023.0f) << 6;
+    else
+        row[x] = (unsigned char)fminf(fmaxf(rintf(code), 0.0f), 255.0f);
+}
+
+// A thread handles one 2x2 luma block and its shared chroma sample.
+// NV12/P010 have identical chroma geometry; all pitches are in bytes.
 extern "C" __global__ void Tonemap_Cuda(
-    unsigned short *src_y, int src_y_linesize,
-    unsigned short *src_uv, int src_uv_linesize,
+    unsigned char *src_y, int src_y_linesize,
+    unsigned char *src_uv, int src_uv_linesize,
     unsigned char *dst_y, int dst_y_linesize,
     unsigned char *dst_uv, int dst_uv_linesize,
     int width, int height,
-    int transfer, float peak, int op, float param, float desat)
+    int transfer, float peak, int op, float param, float desat,
+    int transfer_out, int legacy, int input_depth, int output_depth,
+    float sdr_white, float hdr_peak)
 {
     int xi = blockIdx.x * blockDim.x + threadIdx.x;
     int yi = blockIdx.y * blockDim.y + threadIdx.y;
@@ -186,31 +286,23 @@ extern "C" __global__ void Tonemap_Cuda(
     int x = 2 * xi;
     int y = 2 * yi;
 
-    // P010 stores the 10-bit code in the high bits, so shift right by 6.
-    int syl = src_y_linesize >> 1;
-    int suvl = src_uv_linesize >> 1;
-
-    float y0 = (float)(src_y[(y    ) * syl + x    ] >> 6);
-    float y1 = (float)(src_y[(y    ) * syl + x + 1] >> 6);
-    float y2 = (float)(src_y[(y + 1) * syl + x    ] >> 6);
-    float y3 = (float)(src_y[(y + 1) * syl + x + 1] >> 6);
-
-    float cb = (float)(src_uv[yi * suvl + 2 * xi    ] >> 6);
-    float cr = (float)(src_uv[yi * suvl + 2 * xi + 1] >> 6);
-
-    float3 s0 = rgb2yuv_709(tonemap_rgb(yuv2rgb_2020(y0, cb, cr), transfer, peak, op, param, desat));
-    float3 s1 = rgb2yuv_709(tonemap_rgb(yuv2rgb_2020(y1, cb, cr), transfer, peak, op, param, desat));
-    float3 s2 = rgb2yuv_709(tonemap_rgb(yuv2rgb_2020(y2, cb, cr), transfer, peak, op, param, desat));
-    float3 s3 = rgb2yuv_709(tonemap_rgb(yuv2rgb_2020(y3, cb, cr), transfer, peak, op, param, desat));
-
-    dst_y[(y    ) * dst_y_linesize + x    ] = quant8(219.0f * s0.x + 16.0f);
-    dst_y[(y    ) * dst_y_linesize + x + 1] = quant8(219.0f * s1.x + 16.0f);
-    dst_y[(y + 1) * dst_y_linesize + x    ] = quant8(219.0f * s2.x + 16.0f);
-    dst_y[(y + 1) * dst_y_linesize + x + 1] = quant8(219.0f * s3.x + 16.0f);
-
-    // Average the four chroma samples (matches the default get_chroma_sample).
-    float u = 0.25f * (s0.y + s1.y + s2.y + s3.y);
-    float v = 0.25f * (s0.z + s1.z + s2.z + s3.z);
-    dst_uv[yi * dst_uv_linesize + 2 * xi    ] = quant8(224.0f * u + 128.0f);
-    dst_uv[yi * dst_uv_linesize + 2 * xi + 1] = quant8(224.0f * v + 128.0f);
+    float cb = read_code(src_uv, src_uv_linesize, x, yi, input_depth);
+    float cr = read_code(src_uv, src_uv_linesize, x + 1, yi, input_depth);
+    float u = 0.0f, v = 0.0f;
+    for (int i = 0; i < 4; ++i) {
+        int px = x + (i & 1), py = y + (i >> 1);
+        float code = read_code(src_y, src_y_linesize, px, py, input_depth);
+        float3 c;
+        if (legacy)
+            c = rgb2yuv_709(tonemap_rgb(yuv2rgb_2020(code, cb, cr), transfer, peak, op, param, desat));
+        else
+            c = rgb_to_yuv(convert_rgb(yuv_to_rgb(code, cb, cr, input_depth, transfer),
+                                      transfer, transfer_out, sdr_white, hdr_peak, op, param, desat),
+                           transfer_out);
+        write_code(dst_y, dst_y_linesize, px, py, output_depth, 219.0f * c.x + 16.0f);
+        u += c.y;
+        v += c.z;
+    }
+    write_code(dst_uv, dst_uv_linesize, x, yi, output_depth, 224.0f * (u * 0.25f) + 128.0f);
+    write_code(dst_uv, dst_uv_linesize, x + 1, yi, output_depth, 224.0f * (v * 0.25f) + 128.0f);
 }
-- 
2.55.0

